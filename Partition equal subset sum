Given a non-empty array nums containing only positive integers, find if the array can be partitioned into two subsets such that the sum of elements in both subsets is equal.

 

Example 1:

Input: nums = [1,5,11,5]
Output: true
Explanation: The array can be partitioned as [1, 5, 5] and [11].
Example 2:

Input: nums = [1,2,3,5]
Output: false
Explanation: The array cannot be partitioned into equal sum subsets.



class Solution {
    public boolean canPartition(int[] nums) {
        HashMap <String,Boolean> map = new HashMap<>();
        int total= 0;
        for(int i : nums){
            total+=i;
        }
        return helperFunction(nums, 0,0,map, total);
    }
    public boolean helperFunction(int[] nums, int index, int sum, HashMap<String, Boolean> map, int total){
        if(nums == null || index >= nums.length || sum > total/2){
            return false;
        }
        String counter = index +" "+sum;
        if(sum *2 == total){
            return true;
        }
        if(map.containsKey(counter)){ // to see if already encountered that number
            return map.get(counter);
        }
        Boolean findPartition = helperFunction(nums,index+1,sum,map,total) || helperFunction(nums,index+1,sum + nums[index],map,total);
        map.put(counter,findPartition);
        return findPartition;
    }
}
