Write an efficient algorithm that searches for a value in an m x n matrix. This matrix has the following properties:

Integers in each row are sorted from left to right.
The first integer of each row is greater than the last integer of the previous row.
 

Example 1:


Input: matrix = [[1,3,5,7],[10,11,16,20],[23,30,34,60]], target = 3
Output: true
Example 2:


Input: matrix = [[1,3,5,7],[10,11,16,20],[23,30,34,60]], target = 13
Output: false



// look row by row for the target.
// when find that target < right and target > left then do binary search.
class Solution {
    public boolean searchMatrix(int[][] matrix, int target) {
        if(matrix.length == 0){
            return false;
        }
        int col =matrix[0].length-1;
       // System.out.println(Arrays.toString(matrix[0]));
        for(int i =0;i<matrix.length;i++){
            if(target >= matrix[i][0] && target <= matrix[i][col])
                return binarySearch(matrix,target,i,0,col);
            System.out.println(Arrays.toString(matrix[i]));
        }
        return false;
    }
    public boolean binarySearch(int[][] matrix, int target, int row,int left, int right){
        while(left<=right){
            int mid = left + (right - left)/2;
            if(matrix[row][mid]== target){
                System.out.println(matrix[row][mid]+" matrix");
                System.out.println(target+" target");
                return true;
            }
            else if(target >matrix[row][mid]){
                left = mid+1;
            }
            else{
                right = mid-1;
            }
        }
        return false;
    }
}
