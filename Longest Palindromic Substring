Given a string s, return the longest palindromic substring in s.

 

Example 1:

Input: s = "babad"
Output: "bab"
Note: "aba" is also a valid answer.
Example 2:

Input: s = "cbbd"
Output: "bb"
Example 3:

Input: s = "a"
Output: "a"
Example 4:

Input: s = "ac"
Output: "a"
 

Constraints:

1 <= s.length <= 1000
s consist of only digits and English letters.



class Solution {
    public String longestPalindrome(String s) {
        if(s.length()==1){
            return s;
        }
        int start =0;
        int end =0;
        for(int i =0;i<s.length();i++){
            int len1 = checkPalindrome(s,i,i); // racecar
            int len2 = checkPalindrome(s,i,i+1); // abba
            int len = Math.max(len1,len2); //becasue dont know what type of input (racecare or abba) would need to get max;
            if(len > end-start){ // would do this to save in start & end of the palindrome. need to update the start and end so that it can be returned.
                start= i-(len-1)/2;
                end = i+len/2;
            }
        }
        return s.substring(start,end+1); // +1 because you want to include the end index
    }
    public int checkPalindrome(String s , int left, int right){
        if(left>right || s==null){
            return 0;
        }
        while(left>=0 && right <= s.length()-1 && s.charAt(left) == s.charAt(right)){
            right++;
            left--;
        }
        return  (right-1)-(left+1)+1 ; // +1 for indices. we include 0 with it
    }
}
/*
2 types of palindrome
racecar - odd i,i
abba    - even i,i+1
Need to cater for both types 
*/
